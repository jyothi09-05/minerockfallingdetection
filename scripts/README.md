# Automation Scripts

## Available Scripts
- `setup.ps1` / `setup.sh`: Initializes environment files, directories, and dependencies.
- `run-local.ps1` / `run-local.sh`: Runs backend, frontend, and AI services locally.
- `seed-db.ps1` / `seed-db.sh`: Re-applies database migrations and demo seed records.
